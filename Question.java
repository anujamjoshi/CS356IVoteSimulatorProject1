public interface Question {

	// gets all the answers and stores the amount of times each is there
	public String printResults();
	// prints it out 
	public void addAnswers(Answer a);
	// takes the answers from the students and adds it to the answerCount
}

